# 2026-06-18 工作日志

## 分析最后两次 git commit 的 LLM 服务差异

对比 `895ba45`（first commit）→ `886357e`（feat: add contract analyzer demo platform）中 `frontend/app/api/analyze/route.ts` 的变更，识别出导致 LLM 解析效果下降的三个核心原因：

1. **主因：`max_output_tokens: 8192`** — 原版本无此限制，新版本将输出 token 上限从模型默认硬限到 8192。
2. **次因：System Prompt 指令冲突** — 新增的"紧凑输出"与"全量覆盖"存在矛盾。
3. **次要：temperature=0.2** — 极低温度使模型偏保守。

## 回滚到 v1 + 保留网页配置优先逻辑

- ✅ `route.ts` 已回滚到 v1：system prompt 恢复 6 条、移除 temperature/max_output_tokens/top_p、恢复 contractText 120000 上限、移除 normalizeAnalysisResult/parseModelJson
- ✅ 保留新的模型/Key 选择逻辑：网页端填写并保存后优先使用；留空时 trim → undefined → fallback 到环境变量

